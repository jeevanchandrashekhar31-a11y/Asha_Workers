rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // DANGER: Open access for hackathon ONLY. 
      // Do not use this in production. Anyone can read/write to your database.
      allow read, write: if true;
    }
  }
}
